# Landing demo mockup

Hero visual mirrors the live `/demo` storefront structure (search, campaign banner, new arrivals, categories, product grid, bottom navigation) as a lightweight animated CSS mockup.
